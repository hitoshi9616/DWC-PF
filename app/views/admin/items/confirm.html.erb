<div class="container">

  <div>
    <%= image_tag @item.get_image(100, 100) %>
    <%= @item.name %>
    <%= @item.price %>円
  </div>

  <p>
    この商品に関する情報が完全に消去されます<br />
    本当に削除してもよろしいですか？
  </p>

  <div>
    <%= link_to "削除する", admin_item_path(@item.id), method: :delete %>
  </div>

</div>
