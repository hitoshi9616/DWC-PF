<div class="container">

  <h2>商品詳細</h2>

  <div>
    <%= image_tag @item.get_image(100, 100) %>
    <%= @item.name %>
    <%= @item.price %>円
  </div>

  <div>
    <%= link_to "商品編集", edit_admin_item_path(@item.id) %>
    |
    <%= link_to "商品削除", admin_item_confirm_path(@item.id) %>
  </div>

</div>
