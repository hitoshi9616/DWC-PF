<div class="container">

  <h2 class="mt-3">投稿詳細</h2>

  <div class="row">
    <div class="col-lg-5">
      <h5 class="col text-center mt-2">投稿者：
        <%= link_to admin_customer_path(@post.customer.id), class:"text-dark" do %>
          <strong><%= @post.customer.nickname %></strong>
        <% end %>
      </h5>

      <div class="card">
        <h6 class="card-title p-1">
          タイトル：<strong><%= @post.title %></strong>
        </h6>

        <div class="card-text p-2"><strong>内容</strong>
          <p class="p-1"><%= @post.text %></p>
        </div>
      </div>

      <div class="col text-center mt-4">
        <%= link_to "投稿削除", admin_post_confirm_path(@post.id), class:"btn btn-danger" %>
      </div>
    </div>

    <div class="col-lg-6 ml-2">
      <p>
        コメント件数：<%= @post.post_comments.count %>
      </p>
      <%# 投稿に対するコメントを全て表示する %>
      <% @post.post_comments.each do |post_comment| %>
        <p><%= post_comment.customer.nickname %>
        <%= post_comment.created_at.strftime('%Y/%m/%d') %></p>
        <p><%= post_comment.comment %></p>
        <%= link_to "コメント削除", admin_post_comment_path(post_comment.id), method: :delete, "data-confirm" => "削除しますか？" %>
      <% end %>
    </div>
  </div>

</div>
