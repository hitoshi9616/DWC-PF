<h2 class="text-danger text-center mt-3">
  <strong>レビュー削除確認</strong>
</h2>

<div class="row">
  <div class="col-lg-5 col-md-5 col-sm-12 mb-3">
    <h5 class="col text-center mt-2">
      投稿者：<strong><%= review.customer.nickname %></strong>
    </h5>

    <%= render 'review_confirm', review: @review %>

    <p class="row justify-content-center text-center mt-3">
      <strong>
        このレビューに関連する情報が完全に消去されます<br />
        本当に削除してもよろしいですか？
      </strong>
    </p>

    <div class="col text-center">
      <%= link_to "削除する", admin_review_path(@review.id), method: :delete, class:"btn btn-outline-danger" %>
      <p class="mt-2">削除すると投稿者のページに移動します</p>
    </div>
  </div>

  <div class="col-lg-6 col-md-6 col-sm-12 ml-2">
    <h5 class="text-center">
      コメント件数：<%= @review.review_comments.count %>
    </h5>

    <% if @review.review_comments.count >= 1 %>
      <% @review.review_comments.each do |review_comment| %>
        <%= render 'admin/posts/confirm_comment', comment: review_comment %>
      <% end %>
    <% else %>
      <p class="text-center">このレビューに対するコメントはありません</p>
    <% end %>
  </div>
</div>
