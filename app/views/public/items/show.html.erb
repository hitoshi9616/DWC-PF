<div class="container">

  <div class="row">
    <h2>商品詳細</h2>
  </div>

  <div>
    <div>
      <%= image_tag @item.get_image(100, 100) %>
      <%= @item.name %>
      <%= @item.price %>円
    </div>
  </div>

  <div>
    <%= link_to review_path do%>
      <h4>レビューする</h4>
    <% end %>
  </div>

</div>
